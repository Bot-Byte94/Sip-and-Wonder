import { getChatGPTUser } from '@/app/chatgpt-auth';
import { database,photos } from '@/lib/cafe-db';
export async function GET(req:Request){try{const user=await getChatGPTUser();if(!user)return new Response('Sign in required',{status:401});const id=new URL(req.url).searchParams.get('id');const row=await database().prepare('SELECT photo FROM moments WHERE id=? AND user_id=?').bind(id,user.userId).first<{photo:string|null}>();if(!row?.photo)return new Response('Not found',{status:404});const photo=await photos().get(row.photo);if(!photo)return new Response('Not found',{status:404});return new Response(photo.body,{headers:{'Content-Type':photo.httpMetadata?.contentType??'image/jpeg','Cache-Control':'private, no-store','X-Content-Type-Options':'nosniff'}});}catch(e){console.error('Photo load failed',e);return new Response('Photo unavailable',{status:503});}}
